package main.model.user;

/**
 * The user status enumeration, composed of two states: CUSTOMER and OPERATOR (as of version 0.1).
 *
 * @author Manuel Gallina
 */
public enum UserStatus {
    CUSTOMER,
    OPERATOR
}
